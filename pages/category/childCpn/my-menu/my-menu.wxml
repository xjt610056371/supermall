<view class="list">
  <view wx:for="{{list}}"
        wx:key="index"
        class="item {{currentIndex == index ? 'active' : ''}}"
        bind:tap="clickItem"
        data-index="{{index}}">
    {{item.title}}
  </view>
</view>